services:
  postgres:
    image: postgres:17
    container_name: questbase-postgres
    environment:
      POSTGRES_DB: questbase
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"